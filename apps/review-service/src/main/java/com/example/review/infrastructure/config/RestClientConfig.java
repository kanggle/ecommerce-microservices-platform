package com.example.review.infrastructure.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RestClientConfig {
    // RestClient is created directly in OrderServiceClient with baseUrl injection.
    // This config class is a placeholder for future customizations (timeouts, interceptors).
}
